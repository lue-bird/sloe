if you've cloned the sloe repository, run from this directory with
```bash
cargo run --manifest-path ../Cargo.toml js && node index.mjs
```
if you've installed `sloe`, run with
```bash
sloe js && node index.mjs
```
